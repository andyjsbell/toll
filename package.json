{
  "name": "tolls",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd client && npm install",
    "start": "cd client && npm start",
    "dev": "cd client && PORT=8000 npm start",
    "build": "cd client && npm build",
    "test": "cd client && npm test",
    "eject": "cd client && npm eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
